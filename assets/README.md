# Asset Placeholders

The game draws original pixel-art sprites and backgrounds procedurally in `src/systems/Renderer.js`, and creates all sounds with WebAudio in `src/systems/Audio.js`.

Drop replacement art or audio here if you want to expand it later:

- `sprites/`
- `tiles/`
- `music/`
- `sfx/`
